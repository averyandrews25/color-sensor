# Color Sensor Program for the Raspberry pi
baseline.py, when run by itself will create colors.json, treating the color in front of it as white
display.py, when run, will display the color on the led
